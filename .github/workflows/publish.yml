name: Publish

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version bump"
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: main

      - uses: actions/setup-node@v6
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org/

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: npm ci --no-audit --no-fund --loglevel=error

      - name: Run tests
        working-directory: packages/pi-herdr-orchestrator
        run: bun test

      - name: Bump version
        working-directory: packages/pi-herdr-orchestrator
        run: npm version "${{ inputs.version }}" --git-tag-version=false

      - name: Commit and tag release
        run: |
          VERSION=$(node -p "require('./packages/pi-herdr-orchestrator/package.json').version")
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add packages/pi-herdr-orchestrator/package.json package-lock.json
          git commit -m "chore: release pi-herdr-orchestrator ${VERSION}"
          git tag "v${VERSION}"
          git push origin main --tags

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(node -p "require('./packages/pi-herdr-orchestrator/package.json').version")
          gh release create "v${VERSION}" --generate-notes --title "pi-herdr-orchestrator ${VERSION}"

      - name: Publish to npm
        working-directory: packages/pi-herdr-orchestrator
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Verify publication
        run: npm view "@ret2hell/pi-herdr-orchestrator@$(node -p "require('./packages/pi-herdr-orchestrator/package.json').version")" version
